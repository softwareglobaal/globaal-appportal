# Secrets — NOOIT in git
.env*
!.env.example
# Certificaten & sleutels
certs/
certbot-webroot/
# Logs & runtime-data
logs/
*.log
*.db
**/__pycache__/
*.pyc
.DS_Store
/telefoonregister/
# Eigen repo (softwareglobaal/globaal-organisatie), uitgecheckt op de VM.
/medewerkers/
# Eigen repo (softwareglobaal/globaal-communicatie), uitgecheckt op de VM.
/communicatie/
# Eigen repo (softwareglobaal/globaal-vermogen), uitgecheckt op de VM.
/vermogen/
# Eigen repo (softwareglobaal/globaal-draaiboek), uitgecheckt op de VM.
/draaiboek/
# Eigen repo (softwareglobaal/sales-dashboard), uitgecheckt op de VM.
/sales/
# Eigen repo (softwareglobaal/google-xelion-sync), uitgecheckt op de VM.
/contactsync/
# Geheime bestanden van de contactsync (.env, tokens, sync.db) — nooit in git.
/contactsync-data/
# Eigen repo (softwareglobaal/projecten), uitgecheckt op de VM.
/projecten/
# Installatie-restanten en lokale data op de VM
/aws/
awscliv2.zip
/omv-v2-data/
*.bak-*

# Eigen repo (softwareglobaal/globaal-kosten), uitgecheckt op de VM voor de
# stack-app (herbouw 2026-07-15); de host-pijplijn blijft in ~/kosten.
/kosten/

# Eigen repo (softwareglobaal/angela-site), uitgecheckt op de VM; bouwt de
# containers app-items (beheer) en items-verkoop (angela.sr).
/angela-site/

# Eigen app-repo's, uitgecheckt op de VM (deploy via eigen deploy-stack.sh in cron).
/boekhouding/
/hr/
/monday/
/omv-v2/
# Runtime-data van de agents-app: agents.db + geuploade bijlagen (~1,9 GB). Nooit in git.
/agents-data/
# Gegenereerd door monday/scripts/build-graaf.sh (cron), geen bron.
/monday-graaf/
# Runtime-data van de siyanagents-app (eigen DB + bijlagen). Nooit in git.
/siyanagents-data/
